name: krea
version: 1.1.0
description: "Krea image generation backend (Krea 2 Large + Medium + Medium Turbo foundation models). Requires a direct KREA_API_KEY."
kind: backend
requires_env:
  - KREA_API_KEY
